let sports: string[] = ["Tennis", "Golf", "Cricket"];

// Simplified for loop
for (let currentSport of sports) {
    // Conditionals
    if (currentSport == "Tennis") {
        console.log(currentSport + " is my favorite sport");
    } else {
        console.log(currentSport);
    }
}